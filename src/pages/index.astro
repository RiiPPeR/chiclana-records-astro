---
import Layout from '../layouts/Layout.astro';
import OrbButton from '../components/OrbButton.astro';

const { cookies } = Astro;
const isLoggedIn = cookies.get("sb-access-token") && cookies.get("sb-refresh-token");
---

<Layout title="Chiclana Records">
    <div class="flex flex-col items-center justify-center px-4">
        <div class="text-center mb-12 relative">
            <div class="absolute w-32 h-32 bg-yellow-500 rounded-full -z-10 blur-2xl opacity-30 -top-10 -left-10"></div>
            <div class="absolute w-32 h-32 bg-purple-500 rounded-full -z-10 blur-2xl opacity-30 -bottom-10 -right-10"></div>
            
            <h1 class="text-5xl font-bold text-yellow-500 mb-4">Chiclana Records</h1>
            <p class="text-xl text-gray-300 mb-8 max-w-2xl">
                Tu colección de vinilos digital. Guarda, organiza y comparte tu música favorita en un solo lugar.
            </p>
        </div>

        <div class="flex flex-col sm:flex-row gap-6 items-center justify-center bg-black/40 backdrop-blur-md p-8 rounded-xl shadow-2xl">
            {isLoggedIn ? (
                <a href="/dashboard">
                    <OrbButton 
                        text="Ir a mi colección" 
                        primaryColor="#22c55e" 
                        secondaryColor="#3b82f6"
                    />
                </a>

				<a href="/recordsearch">
					<OrbButton 
						text="Buscar discos" 
						primaryColor="#ef4444" 
						secondaryColor="#8b5cf6"
                    />
                </a>
            ) : (
                <>
                    <a href="/signin">
                        <OrbButton 
                            text="Iniciar sesión" 
                            primaryColor="#ef4444" 
                            secondaryColor="#22c55e"
                        />
                    </a>
                    <a href="/register">
                        <OrbButton 
                            text="Registrarse" 
                            primaryColor="#3b82f6" 
                            secondaryColor="#f59e0b"
                        />
                    </a>
                </>
            )}
        </div>

        <div class="mt-16 grid grid-cols-1 md:grid-cols-3 gap-8 text-center">
            <div class="bg-black/30 backdrop-blur-md p-6 rounded-lg">
                <h3 class="text-yellow-500 text-xl font-semibold mb-2">Colecciona</h3>
                <p class="text-gray-300">Guarda todos tus vinilos favoritos en un solo lugar</p>
            </div>
            <div class="bg-black/30 backdrop-blur-md p-6 rounded-lg">
                <h3 class="text-yellow-500 text-xl font-semibold mb-2">Organiza</h3>
                <p class="text-gray-300">Mantén tu colección ordenada y accesible</p>
            </div>
            <div class="bg-black/30 backdrop-blur-md p-6 rounded-lg">
                <h3 class="text-yellow-500 text-xl font-semibold mb-2">Comparte</h3>
                <p class="text-gray-300">Conecta con otros coleccionistas</p>
            </div>
        </div>
        <div class="mt-8 bg-black/30 backdrop-blur-md p-6 rounded-lg text-center max-w-7xl">
            <h3 class="text-yellow-500 text-xl font-semibold mb-2">Versión Beta</h3>
            <p class="text-gray-300 text-wrap">
                Este proyecto está actualmente en fase beta. Las unicas funciones publicadas son la creación de una cuenta, el inicio de sesión, la busqueda de discos y añadir estos a tu colección. 
                Puedes probar la aplicación usando un usuario de prueba, por si no quieres poner tus datos:
            </p>
            <div class="mt-2 p-3 bg-yellow-500/20 rounded-md inline-block">
                <p class="text-gray-300">
                    Email: <span class="text-blue-400">usuario@usuario.com</span><br/>
                    Contraseña: <span class="text-blue-400">usuario</span>
                </p>
            </div>
            <p class="mt-2 text-gray-300">
                Aunque si quieres puedes crear tu cuenta, no hace falta poner un correo real y la contraseña queda encriptada gracias al modelo de autenticación de <a class="text-blue-300" href="https://supabase.com/">supabase</a>
            </p>
        </div>
    </div>
</Layout>